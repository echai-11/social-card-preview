{
  "name": "social-card-preview",
  "version": "1.0.0",
  "description": "Preview social media cards for links",
  "main": "server/index.js",
  "scripts": {
    "client": "cd client && npm install && npm run build",
    "server": "cd server && npm install && node index.js",
    "start": "npm run client && npm run server",
    "lint": "eslint './**'  --fix-dry-run",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/echai-11/social-card-preview.git"
  },
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/echai-11/social-card-preview/issues"
  },
  "homepage": "https://github.com/echai-11/social-card-preview#readme",
  "dependencies": {},
  "devDependencies": {
    "eslint": "^8.13.0"
  }
}
